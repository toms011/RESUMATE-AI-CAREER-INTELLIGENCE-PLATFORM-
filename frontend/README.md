# Frontend

Place the frontend application source code here (React, Next.js, or similar).

To add your files, extract your zip archive into this directory:

```bash
unzip your-frontend.zip -d frontend/
```
